# Tilt ignore file - allowlist approach
# Ignore everything except essential build files and directories

# Ignore all files in root
/*

# Allow essential files
!go.mod
!go.sum
!Dockerfile
!Tiltfile

# Allow essential directories
!internal/
!cmd/
!api/
!config/

# But within allowed directories, still exclude certain patterns
**/*_test.go
**/zz_generated.*.go
**/*.tmp
**/.DS_Store
**/*.tmp.*
# JetBrains/GoLand temp files during save
**/*.[0-9]*
