---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: kaiwonodes.kaiwo.silogen.ai
spec:
  group: kaiwo.silogen.ai
  names:
    kind: KaiwoNode
    listKind: KaiwoNodeList
    plural: kaiwonodes
    singular: kaiwonode
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.partitioning.appliedProfile
      name: GPU partitioning
      type: string
    - jsonPath: .status.kueueFlavorName
      name: Flavor
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KaiwoNode represents a proxy that Kaiwo uses to interact with
          and report on node statuses.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of the KaiwoNode.
            properties:
              partitioning:
                description: Partitioning configures the node's partitioning
                properties:
                  enabled:
                    default: false
                    description: Enabled enforces partitioning on the node's GPUs.
                      Note that this switches the partitioning changes on or off.
                      If partitioning is first enabled, and then disabled, any changes
                      that were made will persist, but any changes made to the partitioning
                      profile while partitioning is disabled will not take effect.
                    type: boolean
                  profile:
                    description: Profile is the partitioning profile to apply.
                    type: string
                type: object
            type: object
          status:
            description: Status reflects the most recently observed state of the KaiwoNode.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              isControlPlane:
                type: boolean
              kueueFlavorName:
                description: KueueFlavorName is the name of the flavor that Kueue
                  uses for this node
                type: string
              nodeType:
                description: NodeType reports the type of the node
                type: string
              partitioning:
                properties:
                  appliedProfile:
                    description: AppliedProfile reports the currently active profile
                    type: string
                  phase:
                    description: Phase reports the current partitioning phase
                    type: string
                type: object
              resources:
                description: Resources reports the resources that the node currently
                  has
                properties:
                  cpu:
                    properties:
                      actual:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      nominal:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    type: object
                  gpus:
                    properties:
                      isPartitioned:
                        description: |-
                          IsPartitioned reports whether the GPUs are currently partitioned or not. A null value implicates that it is unclear
                          whether the GPUs are partitioned or not
                        type: boolean
                      logicalCount:
                        description: LogicalCount is the number of logical GPUs in
                          the node and is taken from the Kubernetes node status capacity.
                          If the GPUs are not partitioned, this is the same as PhysicalCount
                        type: integer
                      logicalVramPerGpu:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          LogicalVramPerGpu is the vRAM that each logical GPU sees. If the node's GPUs are not partitioned, this is the
                          total vRAM that each GPU has. If the node's GPUs are partitioned, this is the vRAM that each partition has.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      model:
                        description: Model is the model name of the GPU
                        type: string
                      physicalCount:
                        description: PhysicalCount is the number of physical GPUs
                          in the node
                        type: integer
                      physicalVramPerGpu:
                        anyOf:
                        - type: integer
                        - type: string
                        description: PhysicalVramPerGpu is the vRAM that each physical
                          GPU has.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      resourceName:
                        description: ResourceName is the GPU resource name for this
                          GPU
                        type: string
                      vendor:
                        description: Vendor is the vendor of the GPU (primarily `amd`
                          or `nvidia`)
                        type: string
                    type: object
                  memory:
                    properties:
                      actual:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      nominal:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    type: object
                type: object
              status:
                default: Unknown
                description: KaiwoNodeStatus reports the current status for the node
                type: string
              unschedulable:
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
