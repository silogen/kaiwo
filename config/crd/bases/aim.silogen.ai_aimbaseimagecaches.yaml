---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: aimbaseimagecaches.aim.silogen.ai
spec:
  group: aim.silogen.ai
  names:
    categories:
    - aim
    - all
    kind: AIMBaseImageCache
    listKind: AIMBaseImageCacheList
    plural: aimbaseimagecaches
    shortNames:
    - aimbic
    singular: aimbaseimagecache
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .status.nodesCached
      name: Cached
      type: integer
    - jsonPath: .status.nodesTotal
      name: Total
      type: integer
    - jsonPath: .status.refCount
      name: Refs
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AIMBaseImageCache defines a DaemonSet-backed cache for a base
          container image.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIMBaseImageCacheSpec defines desired caching behaviour for
              a base image digest.
            properties:
              image:
                description: Image is the base container image (ideally pinned by
                  digest) that should be cached cluster-wide.
                minLength: 1
                type: string
              imagePullSecrets:
                description: ImagePullSecrets contains registry pull secrets attached
                  to the caching pods.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              nodeSelector:
                additionalProperties:
                  type: string
                description: NodeSelector restricts the nodes where caching pods are
                  scheduled.
                type: object
              parallelismLimit:
                default: 5
                description: ParallelismLimit controls the maximum number of concurrent
                  node pulls.
                format: int32
                minimum: 1
                type: integer
              serviceAccountName:
                description: ServiceAccountName specifies the service account used
                  by the caching DaemonSet.
                type: string
            required:
            - image
            type: object
          status:
            description: AIMBaseImageCacheStatus reflects the observed state of the
              cache.
            properties:
              conditions:
                description: Conditions provides high-level cache readiness and error
                  states.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              nodesCached:
                description: NodesCached tracks how many nodes currently have the
                  base image cached.
                format: int32
                type: integer
              nodesFailed:
                description: NodesFailed tracks nodes where caching failed.
                format: int32
                type: integer
              nodesTotal:
                description: NodesTotal tracks how many nodes are targeted by the
                  cache.
                format: int32
                type: integer
              observedGeneration:
                description: ObservedGeneration is the last processed generation.
                format: int64
                type: integer
              refCount:
                description: RefCount summarises the number of active references.
                format: int32
                type: integer
              refs:
                description: Refs enumerates resources currently referencing this
                  cache.
                items:
                  description: AIMBaseImageCacheReference captures a consumer referencing
                    the cache.
                  properties:
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    uid:
                      description: |-
                        UID is a type that holds unique ID values, including UUIDs.  Because we
                        don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                        intent and helps make sure that UIDs and names do not get conflated.
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
