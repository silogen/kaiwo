---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: partitioningprofiles.infrastructure.silogen.ai
spec:
  group: infrastructure.silogen.ai
  names:
    categories:
    - infrastructure
    - gpu
    kind: PartitioningProfile
    listKind: PartitioningProfileList
    plural: partitioningprofiles
    shortNames:
    - gpuprofile
    - pp
    singular: partitioningprofile
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: Display Name
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          PartitioningProfile is the Schema for the partitioningprofiles API.
          It defines a reusable GPU partition configuration that can be referenced by PartitioningPlans.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PartitioningProfileSpec defines the desired state of PartitioningProfile.
            properties:
              displayName:
                description: DisplayName is a human-readable description of this profile.
                minLength: 1
                type: string
              expectedResources:
                description: |-
                  ExpectedResources defines the resources that should appear in
                  node.status.allocatable after partitioning succeeds.
                items:
                  description: ExpectedResource defines an expected resource after
                    partitioning.
                  properties:
                    count:
                      description: Count is the expected number of resources.
                      format: int32
                      minimum: 0
                      type: integer
                    name:
                      description: Name is the resource name (e.g., "amd.com/gpu").
                      minLength: 1
                      type: string
                  required:
                  - count
                  - name
                  type: object
                type: array
              operatorPayload:
                description: |-
                  OperatorPayload contains the GPU operator configuration to apply.
                  This is typically a reference to a ConfigMap containing the DCM config.json.
                properties:
                  inline:
                    description: |-
                      Inline payload data (arbitrary JSON).
                      Required when Kind is Inline.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  kind:
                    default: ConfigMap
                    description: Kind of the reference (ConfigMap, Secret, or Inline).
                    enum:
                    - ConfigMap
                    - Secret
                    - Inline
                    type: string
                  name:
                    description: |-
                      Name of the ConfigMap or Secret.
                      Required when Kind is ConfigMap or Secret.
                    type: string
                  namespace:
                    description: |-
                      Namespace of the ConfigMap or Secret.
                      Required when Kind is ConfigMap or Secret.
                    type: string
                type: object
              targetSelector:
                description: |-
                  TargetSelector is an optional guardrail to ensure the profile is only
                  applied to compatible nodes. If specified, the controller will validate
                  that nodes match this selector before applying the profile.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              verification:
                description: Verification defines how to verify that partitioning
                  succeeded.
                properties:
                  gpuReadyLabel:
                    default: amd.com/gpu.ready
                    description: |-
                      GpuReadyLabel is the node label that indicates GPU readiness.
                      The controller waits for this label to be present with value "true".
                    type: string
                  requirePluginResources:
                    description: |-
                      RequirePluginResources is a list of resource names that must appear
                      in node.status.allocatable after partitioning.
                    items:
                      type: string
                    type: array
                  timeoutSeconds:
                    default: 900
                    description: TimeoutSeconds is the maximum time to wait for verification
                      to pass.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
            required:
            - displayName
            - operatorPayload
            type: object
          status:
            description: PartitioningProfileStatus defines the observed state of PartitioningProfile.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the profile's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
