---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: aimimages.aim.silogen.ai
spec:
  group: aim.silogen.ai
  names:
    categories:
    - aim
    - all
    kind: AIMImage
    listKind: AIMImageList
    plural: aimimages
    shortNames:
    - aimclimg
    singular: aimimage
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Model Name
      type: string
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AIMImage is the Schema for cluster-scoped AIM image catalog entries.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIMImageSpec defines the desired state of AIMImage.
            properties:
              defaultServiceTemplate:
                description: |-
                  DefaultServiceTemplate is the name of the default service template to use, if an
                  AIMService is created without specifying a template name.
                type: string
              image:
                description: |-
                  Image is the container image URI for this AIM model.
                  This image is inspected by the operator to select runtime profiles used by templates.
                minLength: 1
                type: string
              modelId:
                description: |-
                  ModelID is the ID name (includes version/revision).
                  Example: `meta/llama-3-8b:1.1+20240915`.
                minLength: 1
                type: string
            required:
            - defaultServiceTemplate
            - image
            - modelId
            type: object
          status:
            description: AIMImageSpec defines the desired state of AIMImage.
            properties:
              defaultServiceTemplate:
                description: |-
                  DefaultServiceTemplate is the name of the default service template to use, if an
                  AIMService is created without specifying a template name.
                type: string
              image:
                description: |-
                  Image is the container image URI for this AIM model.
                  This image is inspected by the operator to select runtime profiles used by templates.
                minLength: 1
                type: string
              modelId:
                description: |-
                  ModelID is the ID name (includes version/revision).
                  Example: `meta/llama-3-8b:1.1+20240915`.
                minLength: 1
                type: string
            required:
            - defaultServiceTemplate
            - image
            - modelId
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
