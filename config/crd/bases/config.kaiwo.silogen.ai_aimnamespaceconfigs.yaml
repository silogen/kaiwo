---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: aimnamespaceconfigs.config.kaiwo.silogen.ai
spec:
  group: config.kaiwo.silogen.ai
  names:
    categories:
    - kaiwo
    - all
    kind: AIMNamespaceConfig
    listKind: AIMNamespaceConfigList
    plural: aimnamespaceconfigs
    shortNames:
    - aimns
    singular: aimnamespaceconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.gateway.name
      name: Gateway
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AIMNamespaceConfig configures credentials and routing for a namespace.
          It is namespaced and typically created by a tenant administrator.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: "AIMNamespaceConfigSpec defines the desired configuration
              for an AIM-enabled namespace.\n\n- `gateway`: KGateway instance to use
              for exposure and routing\n- `credentials`: secret references for model
              sources and caches (HF/S3/GCS/Azure)\n\nExample:\n\n```yaml\ngateway:\n\n\tname:
              kgw-default\n\tnamespace: gateway-system\n\ncredentials:\n\n\thuggingFaceToken:\n\t
              \ secretName: hf-creds\n\t  secretKey: token\n\ts3:\n\t  - endpointUrl:
              https://s3.us-east-1.amazonaws."
            properties:
              credentials:
                description: Credentials provide secret references used during model
                  access and caching.
                properties:
                  azureBlob:
                    description: AzureBlob contains one or more Azure Blob credential
                      configurations.
                    items:
                      description: AIMAzureBlobCredential configures an Azure Blob
                        Storage connection string reference.
                      properties:
                        connectionString:
                          description: |-
                            SecretValueReference references a key within a Kubernetes Secret.

                            Example:

                            ```yaml
                            secretName: hf-creds
                            secretKey: token
                            ```
                          properties:
                            secretKey:
                              description: SecretKey is the key within the Secret.
                              type: string
                            secretName:
                              description: SecretName is the name of the Secret.
                              type: string
                          type: object
                      type: object
                    type: array
                  gcs:
                    description: GCS contains one or more GCS credential configurations.
                    items:
                      description: |-
                        AIMGCSCredential configures a GCS service account credential reference.
                        The referenced secret key should contain the JSON content of the service account.
                      properties:
                        applicationCredentials:
                          description: |-
                            SecretValueReference references a key within a Kubernetes Secret.

                            Example:

                            ```yaml
                            secretName: hf-creds
                            secretKey: token
                            ```
                          properties:
                            secretKey:
                              description: SecretKey is the key within the Secret.
                              type: string
                            secretName:
                              description: SecretName is the name of the Secret.
                              type: string
                          type: object
                      type: object
                    type: array
                  huggingFaceToken:
                    description: HuggingFaceToken is the token used to access Hugging
                      Face repositories.
                    properties:
                      secretKey:
                        description: SecretKey is the key within the Secret.
                        type: string
                      secretName:
                        description: SecretName is the name of the Secret.
                        type: string
                    type: object
                  s3:
                    description: S3 contains one or more S3 credential configurations.
                    items:
                      description: |-
                        AIMS3Credential configures S3 credential references for a specific endpoint.
                        Provide an endpoint and secret references for authentication.
                      properties:
                        accessKeyId:
                          description: |-
                            SecretValueReference references a key within a Kubernetes Secret.

                            Example:

                            ```yaml
                            secretName: hf-creds
                            secretKey: token
                            ```
                          properties:
                            secretKey:
                              description: SecretKey is the key within the Secret.
                              type: string
                            secretName:
                              description: SecretName is the name of the Secret.
                              type: string
                          type: object
                        endpointUrl:
                          type: string
                        secretAccessKey:
                          description: |-
                            SecretValueReference references a key within a Kubernetes Secret.

                            Example:

                            ```yaml
                            secretName: hf-creds
                            secretKey: token
                            ```
                          properties:
                            secretKey:
                              description: SecretKey is the key within the Secret.
                              type: string
                            secretName:
                              description: SecretName is the name of the Secret.
                              type: string
                          type: object
                      type: object
                    type: array
                type: object
              gateway:
                description: Gateway references the KGateway instance to use for exposure
                  and routing.
                properties:
                  name:
                    description: Name is the name of the gateway object.
                    type: string
                  namespace:
                    description: Namespace is the namespace where the gateway object
                      resides.
                    type: string
                type: object
              images:
                description: Images configures image pull behavior for AIM images
                  within this namespace.
                properties:
                  imagePullSecrets:
                    description: ImagePullSecrets is the list of Secret names to use
                      for pulling AIM images.
                    items:
                      type: string
                    type: array
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
