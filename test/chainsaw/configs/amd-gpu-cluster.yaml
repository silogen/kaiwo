apiVersion: chainsaw.kyverno.io/v1alpha2
kind: Configuration
metadata:
  name: amd-gpu-cluster
spec:
  execution:
    parallel: 30
  discovery:
    fullName: true
  timeouts:
    apply: 10s
    assert: 90s
    cleanup: 120s
    delete: 90s
    error: 60s
    exec: 10s
  error:
    catch:
    - command:
        entrypoint: log
        env:
        - name: NAMESPACE
          value: ($namespace)
        args:
        - $NAMESPACE
        - --namespaced-level=debug
        - --cluster-level=info
