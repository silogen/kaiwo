apiVersion: chainsaw.kyverno.io/v1alpha2
kind: Configuration
metadata:
  name: ci-config
spec:
  parallel: 30
  timeouts:
    apply: 10s
    assert: 90s
    cleanup: 60s
    delete: 90s
    error: 60s
    exec: 10s
