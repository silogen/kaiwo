# GPU preemption E2E test values.
# Set total_gpus to the total amd.com/gpu count in the cluster.
#
# Run from test/:
#   chainsaw test \
#     --config chainsaw/configs/gpu-preemption.yaml \
#     --test-dir chainsaw/tests/gpu-preemption \
#     --values chainsaw/values/gpu-preemption.yaml \
#     --parallel 1
total_gpus: 8
print_level: debug
