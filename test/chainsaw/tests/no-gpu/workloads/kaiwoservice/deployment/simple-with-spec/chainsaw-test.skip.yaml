# This test is skipped for now until more debugging can be done
apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: kaiwoservice-reconciliation-with-deployment-spec-labels
spec:
  timeouts:
    assert: 90s
    cleanup: 60s
  steps:
  - try:
    - apply:
        file: basic-kaiwoservice-with-deployment-spec.yaml
    - assert:
        resource:
          apiVersion: kaiwo.silogen.ai/v1alpha1
          kind: KaiwoService
          metadata:
            name: kaiwoservice-2
          status:
            status: RUNNING
    - assert:
        resource:
          apiVersion: apps/v1
          kind: Deployment
          metadata:
            name: kaiwoservice-2
          status:
            availableReplicas: 1
            readyReplicas: 1
            replicas: 1
