apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: scheduling-negative-gpu-count-vram-mismatch
spec:
  steps:
  - try:
    - apply:
        file: job.yaml
    - wait:
        apiVersion: kaiwo.silogen.ai/v1alpha1
        kind: KaiwoJob
        name: kaiwo-job
        timeout: 10s
        for:
          condition:
            name: Schedulable
            value: 'false'
