apiVersion: batch/v1
kind: Job
metadata:
  name: gpu-preemption-multi-pod
  annotations:
    kaiwo.silogen.ai/gpu-preemption.enabled: "true"
spec:
  parallelism: 2
  completions: 2
  template:
    spec:
      containers:
      - name: workload
        image: busybox:latest
        command: ["sleep", "600"]
        resources:
          requests:
            amd.com/gpu: "1"
          limits:
            amd.com/gpu: "1"
      restartPolicy: Never
