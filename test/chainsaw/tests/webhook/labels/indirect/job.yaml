apiVersion: batch/v1
kind: Job
metadata:
  name: label-propagation-indirect-job
  labels:
    foo: bar
    kaiwo.silogen.ai/managed: "true"
spec:
  template:
    spec:
      containers:
      - name: busybox
        image: busybox:latest
        command: ["sleep", "1"]
      restartPolicy: Never
