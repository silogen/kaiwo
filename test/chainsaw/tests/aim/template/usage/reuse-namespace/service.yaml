apiVersion: aim.silogen.ai/v1alpha1
kind: AIMService
metadata:
  name: reuse-namespace-template
spec:
  model:
    ref: reuse-namespace
  overrides:
    metric: throughput
    precision: fp16
    gpuSelector:
      count: 1
      model: MI300X
