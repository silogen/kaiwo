apiVersion: aim.silogen.ai/v1alpha1
kind: AIMService
metadata:
  name: test-precision-override-service
spec:
  model:
    ref: test-precision-override
  # Override precision to fp16 - should filter out fp8 template
  overrides:
    precision: fp16
