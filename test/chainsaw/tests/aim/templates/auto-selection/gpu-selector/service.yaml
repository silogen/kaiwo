apiVersion: aim.silogen.ai/v1alpha1
kind: AIMService
metadata:
  name: test-gpu-selector-service
spec:
  model:
    ref: test-gpu-selector
  # Override GPU selector to MI300X - should filter out MI250X template
  overrides:
    gpuSelector:
      model: MI300X
      count: 1
