apiVersion: aim.silogen.ai/v1alpha1
kind: AIMService
metadata:
  name: default-name-service
spec:
  model:
    ref: default-testing-llama-3-8b
  kvCache:
    # name not specified - should default to "kvcache-{service-name}"
    type: redis
    storage:
      storageClassName: openebs-hostpath


