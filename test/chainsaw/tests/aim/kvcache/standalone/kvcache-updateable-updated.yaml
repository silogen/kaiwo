apiVersion: aim.silogen.ai/v1alpha1
kind: AIMKVCache
metadata:
  name: updateable-kvcache
spec:
  kvCacheType: redis
  resources:
    requests:
      cpu: "1"
      memory: "1Gi"
    limits:
      cpu: "1"
      memory: "1Gi"
  storage:
    storageClassName: ($values.storageClass)

