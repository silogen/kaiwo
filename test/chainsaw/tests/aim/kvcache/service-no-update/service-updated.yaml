apiVersion: aim.silogen.ai/v1alpha1
kind: AIMService
metadata:
  name: service-no-update-test
spec:
  model:
    ref: default-testing-llama-3-8b
  kvCache:
    name: kvcache-service-no-update
    type: redis
    storage:
      size: 1Gi  # Same as original (storage changes would be ignored anyway)
      storageClassName: ($values.storageClass)
    resources:
      requests:
        cpu: "2"       # CHANGED - but should be IGNORED
        memory: "2Gi"  # CHANGED - but should be IGNORED
      limits:
        cpu: "4"       # CHANGED - but should be IGNORED
        memory: "4Gi"  # CHANGED - but should be IGNORED

