apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: basic-creation-from-image-url
spec:
  description: Test that AIMService can be created by only specifying the Docker container image URL
  steps:
  - name: Create AIMService
    try:
    - apply:
        file: service.yaml

  - name: Verify the model was created and is ready
    try:
    - assert:
        resource:
          apiVersion: aim.silogen.ai/v1alpha1
          kind: AIMModel
          metadata:
            labels:
              aim.silogen.ai/model.image: ghcr.io-silogen-aim-dummy-0.1.3
          status:
            status: Ready
