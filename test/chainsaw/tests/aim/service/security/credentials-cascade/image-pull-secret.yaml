apiVersion: v1
kind: Secret
metadata:
  name: test-registry-secret
type: kubernetes.io/dockerconfigjson
data:
  # Empty dockerconfigjson - not needed for test validation, just for resource existence
  .dockerconfigjson: e30K
