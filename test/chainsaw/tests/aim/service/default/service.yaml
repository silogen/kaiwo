apiVersion: aim.silogen.ai/v1alpha1
kind: AIMService
metadata:
  name: test
spec:
  aimImageName: test
  templateRef: test
  routing:
    enabled: true
    gatewayRef:
      name: kserve-ingress-gateway
      namespace: kgateway-system
