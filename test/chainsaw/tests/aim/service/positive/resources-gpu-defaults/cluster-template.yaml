apiVersion: aim.silogen.ai/v1alpha1
kind: AIMClusterServiceTemplate
metadata:
  name: gpu-defaults-template
spec:
  aimModelName: gpu-defaults-image
  metric: latency
  gpuSelector:
    count: 1
    model: MI300X
