apiVersion: aim.silogen.ai/v1alpha1
kind: AIMClusterRuntimeConfig
metadata:
  name: merge-config
spec:
  defaultStorageClassName: slow-storage
  routing:
    enabled: true
    gatewayRef:
      name: kserve-ingress-gateway
      namespace: kgateway-system
    routeTemplate: "/cluster-merge/{.metadata.name}"
