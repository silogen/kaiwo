apiVersion: aim.silogen.ai/v1alpha1
kind: AIMClusterModel
metadata:
  name: default-testing-llama-3-8b
spec:
  image: ghcr.io/silogen/aim-dummy:0.1.3
  defaultServiceTemplate: default-testing-llama-3-8b
  # Don't auto-generate templates, since we just need a single manual one
  discovery:
    enabled: false
  resources:
    limits:
      memory: 2Gi
      #amd.com/gpu: '1'
    requests:
      cpu: '100m'
      memory: 1Gi
      #amd.com/gpu: '1'
