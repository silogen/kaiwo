apiVersion: aim.silogen.ai/v1alpha1
kind: AIMService
metadata:
  name: test-service
spec:
  model:
    ref: default-testing-llama-3-8b
  runtimeConfigName: routing-template-service
  routing:
    enabled: true
    gatewayRef:
      name: kserve-ingress-gateway
      namespace: kgateway-system
    pathTemplate: "/custom/{.metadata.namespace}/{.metadata.name}/"
