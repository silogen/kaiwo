apiVersion: aim.silogen.ai/v1alpha1
kind: AIMService
metadata:
  name: test-service
spec:
  aimImageName: meta/route-invalid
  templateRef: llama-3-8b-latency-routing-template-invalid
  runtimeConfigName: routing-template-invalid
  routing:
    enabled: true
    gatewayRef:
      name: kserve-ingress-gateway
      namespace: kgateway-system
