apiVersion: aim.silogen.ai/v1alpha1
kind: AIMService
metadata:
  name: test-service
spec:
  aimImageName: llama-3-8b-deletion
  templateRef: llama-3-8b-latency-deletion
  replicas: 1
  routing:
    enabled: true
    gatewayRef:
      name: kserve-ingress-gateway
      namespace: kgateway-system
