apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: template-not-found
spec:
  description: Test that service fails when referenced template does not exist
  timeouts:
    assert: 60s
  steps:

  - name: Create AIMService referencing non-existent template
    try:
    - apply:
        file: service.yaml

  - name: Verify service fails with TemplateNotFound
    try:
    - assert:
        timeout: 30s
        resource:
          apiVersion: aim.silogen.ai/v1alpha1
          kind: AIMService
          metadata:
            name: test-service
          status:
            status: Failed
            (conditions[?type == 'Resolved']):
            - status: "False"
              reason: TemplateNotFound

  - name: Verify no InferenceService was created
    try:
    - error:
        resource:
          apiVersion: serving.kserve.io/v1beta1
          kind: InferenceService
          metadata:
            name: test-service
