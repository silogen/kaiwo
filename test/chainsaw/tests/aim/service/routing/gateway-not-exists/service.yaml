apiVersion: aim.silogen.ai/v1alpha1
kind: AIMService
metadata:
  name: test-service
spec:
  model:
    ref: default-testing
  routing:
    enabled: true
    gatewayRef:
      name: non-existent-gateway
      namespace: non-existent-namespace
