apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards-kaiwo
  namespace: test-observability
  labels:
    grafana_dashboard: "1"
data:
  namespace-debug-dashboard.json: |
{{ .Files.Get "namespace-debug-dashboard.json" | indent 4 }}
  cluster-health-dashboard.json: |
{{ .Files.Get "cluster-health-dashboard.json" | indent 4 }}
